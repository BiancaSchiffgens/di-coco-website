---
import { getEntry } from "astro:content";
import ServiceLayout from "../layouts/ServiceLayout.astro";

const entry = await getEntry("services", "linkedin-account-management");
if (!entry) throw new Error("Service entry not found: linkedin-account-management");
---

<ServiceLayout entry={entry} />
